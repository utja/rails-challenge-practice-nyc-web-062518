<h1>Create a New Company</h1>

<%= form_for @company do |f| %>
<%= f.label :name %>
<%= f.text_field :name %><br>

<%= f.collection_select :building_ids, Building.all, :id, :name %>
<br>
BUILDINGS / FLOORS

Make a new company

Pick multiple offices(floors) from multiple buildings

HOW DO WE SHOW THE AVAILABLE FLOORS FOR THAT BUILDING?


Once created ->

view pages should list all offices and buildings and employees_id

Edit pages should be able to edit the employees for that company
<br>
<%= f.fields_for :buildings do |building| %>
  <%= building.label :name %>
  <%= building.text_field :name %><br>

  <%= building.label :name %>
  <%= building.text_field :name %><br>
<% end %>

<% end %>
